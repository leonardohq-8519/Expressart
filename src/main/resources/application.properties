spring.application.name = Expressart

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.jpa.hibernate.ddl-auto=validate

spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}